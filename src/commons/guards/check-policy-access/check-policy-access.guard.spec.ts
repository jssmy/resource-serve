import { CheckPolicyAccessGuard } from './check-policy-access.guard';

describe('CheckPolicyAccessGuard', () => {
  it('should be defined', () => {
    expect(new CheckPolicyAccessGuard()).toBeDefined();
  });
});
